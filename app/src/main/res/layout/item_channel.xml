<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/bg_tv_focus_item"
    android:clickable="true"
    android:focusable="true"
    android:gravity="center_horizontal"
    android:orientation="vertical"
    android:padding="8dp">

    <FrameLayout
        android:layout_width="120dp"
        android:layout_height="84dp"
        android:background="@drawable/bg_channel_thumb">

        <!-- Fallback monogram (always present, hidden when logo loads) -->
        <TextView
            android:id="@+id/tv_logo_fallback"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:fontFamily="sans-serif-black"
            android:gravity="center"
            android:letterSpacing="0.04"
            android:maxLines="1"
            android:padding="6dp"
            android:shadowColor="#7F22D3EE"
            android:shadowDx="0"
            android:shadowDy="0"
            android:shadowRadius="10"
            android:textColor="#FF22D3EE"
            android:textSize="22sp"
            tools:ignore="HardcodedText"
            xmlns:tools="http://schemas.android.com/tools"
            tools:text="TV" />

        <ImageView
            android:id="@+id/iv_logo"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:contentDescription="Logo"
            android:padding="4dp"
            android:scaleType="fitCenter" />

        <ImageView
            android:id="@+id/iv_favorite_star"
            android:layout_width="18dp"
            android:layout_height="18dp"
            android:layout_gravity="top|end"
            android:layout_margin="3dp"
            android:contentDescription="Favorite"
            android:src="@drawable/ic_star_filled"
            android:visibility="gone" />

        <TextView
            android:id="@+id/tv_drm_badge"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom|end"
            android:layout_margin="3dp"
            android:background="@drawable/bg_badge_drm"
            android:fontFamily="sans-serif-medium"
            android:paddingStart="5dp"
            android:paddingEnd="5dp"
            android:paddingTop="1dp"
            android:paddingBottom="1dp"
            android:text="DRM"
            android:textColor="@color/white"
            android:textSize="8sp"
            android:visibility="gone" />
    </FrameLayout>

    <TextView
        android:id="@+id/tv_name"
        android:layout_width="120dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:ellipsize="end"
        android:fontFamily="sans-serif-medium"
        android:gravity="center"
        android:maxLines="2"
        android:textColor="@color/text_primary"
        android:textSize="11sp" />
</LinearLayout>
